pipeline:
  name: employee-processor-dataflow
  identifier: employeeprocessordataflow
  projectIdentifier: default_project
  orgIdentifier: default
  tags: {}
  properties:
    ci:
      codebase:
        connectorRef: itsavvyankurgithub
        repoName: employee-processor-dataflow
        build: <+input>
  stages:
    - stage:
        name: build
        identifier: build
        description: ""
        type: CI
        spec:
          cloneCodebase: true
          platform:
            os: Linux
            arch: Amd64
          runtime:
            type: Cloud
            spec: {}
          execution:
            steps:
              - step:
                  type: Run
                  name: mvn_clean_install
                  identifier: mvn_clean_install
                  spec:
                    connectorRef: account.harnessImage
                    image: maven:3.8.5-openjdk-18
                    shell: Sh
                    command: mvn clean install
